# Invalid pod - should be rejected (invalid name with uppercase)
apiVersion: v1
kind: Pod
metadata:
  name: InvalidName
  namespace: default
spec:
  containers:
    - name: nginx
      image: nginx:1.21
